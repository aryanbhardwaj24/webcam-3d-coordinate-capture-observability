Legacy research pipeline archive

This directory preserves the original Python and Docker computer vision research engine that powered the project before the client-side WebAssembly migration.

It contains the earlier hardware-constrained R&D pipeline, including calibration assets, backend services, export helpers, and supporting scripts that informed the later frontend architecture.
